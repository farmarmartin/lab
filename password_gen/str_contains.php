<?php
$small_letters = implode(range("a", "z"));
$big_letters = implode(range("A", "Z"));
$numbers = implode(range(0, 9));
$string = "asfhaHf8";

function complexityTest(string $password, string...$parameters)
{
    $points = 0;

    for ($i = 0; $i < strlen($password); $i++) {
        
        
    }
    echo "\norigo: " . $password;
    echo "\n".$points;
}

?> 